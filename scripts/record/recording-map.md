# 电影宣传片录制地图

主演示只走一条连续因果链：在环境设置中选择官方 DeepSeek Harness 与 DeepSeek V4 Pro，打开隔离的 ImageModelArena 工作区，新建会话并提交真实开发任务，最后查看文件变化和验证结果。

镜头中的工作区输入统一使用 `/tmp/ff-deepseek-film/ImageModelArena`。它指向本片专用隔离副本，不暴露用户名，也不会修改用户原项目。

核心结果证据为工作台从“任务未完成”变为“任务已完成”，并在同一轮 Run 中出现工具调用、文件变化和验证输出。斜杠命令只打开真实目录，不执行会改变会话状态的命令。

本机同一 Bridge 已有 6 次官方 DeepSeek Harness 真实验收记录，均通过完整事件链校验。本片付费任务仍只允许提交一次，失败不自动重试。
